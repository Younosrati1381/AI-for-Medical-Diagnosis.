🩺 پروژه طبقه‌بندی اشعه ایکس قفسه سینه با استفاده از یادگیری عمیق

این پروژه یک مدل یادگیری عمیق برای تشخیص خودکار بیماری پنومونی (ذات‌الریه) از تصاویر اشعه ایکس قفسه سینه ارائه می‌دهد.

📋 فهرست مطالب

· مقدمه
· داده‌ها
· معماری مدل
· نتایج
· نحوه اجرا
· محدودیت‌ها
· مراجع

🎯 مقدمه

این پروژه با هدف توسعه یک سیستم تشخیص خودکار برای بیماری پنومونی از تصاویر اشعه ایکس قفسه سینه طراحی شده است. مدل ارائه شده از معماری CNN استفاده کرده و برای کار روی CPU بهینه‌سازی شده است.

📊 داده‌ها

داده‌های استفاده شده شامل تصاویر اشعه ایکس قفسه سینه است که به دو دسته تقسیم می‌شوند:

· NORMAL: تصاویر طبیعی قفسه سینه
· PNEUMONIA: تصاویر مبتلا به پنومونی

آمار داده‌ها:

· داده‌های آموزشی: 4187 نمونه
· داده‌های اعتبارسنجی: 1045 نمونه
· داده‌های تست: 624 نمونه

توازن کلاس‌ها:

· وزن کلاس NORMAL: 1.94
· وزن کلاس PNEUMONIA: 0.67

🧠 معماری مدل

مدل از یک شبکه کانولوشنی عمیق با معماری زیر استفاده می‌کند:

مشخصات کلی:

· سایز تصویر ورودی: 128×128 پیکسل
· حجم بچ: 28
· تابع فعال‌سازی: ReLU (لایه‌های میانی)، Sigmoid (لایه خروجی)
· تابع هزینه: Binary Cross-Entropy
· بهینه‌ساز: Adam با نرخ یادگیری 0.0005

ساختار لایه‌ها:

1. بلوک‌های کانولوشنی متوالی با نرمال‌سازی دسته‌ای
2. لایه‌های MaxPooling برای کاهش ابعاد
3. لایه‌های Dropout برای جلوگیری از بیش‌برازش
4. لایه‌های چگالی (Dense) برای طبقه‌بندی نهایی

آمار پارامترها:

· پارامترهای قابل آموزش: 4,811,937
· پارامترهای غیرقابل آموزش: 2,176
· حجم کل مدل: 18.36 مگابایت

📈 آموزش مدل

تنظیمات آموزش:

· تعداد دوره‌ها: 30
· Callback‌ها:
  · EarlyStopping (صبر: 8 دوره)
  · ReduceLROnPlateau (فاکتور: 0.5، صبر: 4 دوره)
  · ModelCheckpoint (ذخیره بهترین مدل)

استراتژی‌های مقابله با بیش‌برازش:

1. افزایش داده‌ها (Data Augmentation)
2. نرمال‌سازی دسته‌ای
3. Dropout با نرخ‌های مختلف
4. وزن‌دهی کلاس‌ها برای مقابله با عدم توازن

🎯 نتایج

معیارهای ارزیابی روی داده تست:

· دقت (Accuracy): 0.90 (90%)
· پیش‌بینی (Precision): 0.89
· احساس (Recall): 0.96
· ناحیه زیر منحنی (AUC): 0.96
· امتیاز F1: 0.92

ماتریس درهم‌ریختگی:

```
[[TN  FP]
 [FN  TP]]
```

🚀 نحوه اجرا

پیش‌نیازها:

```bash
pip install tensorflow numpy matplotlib scikit-learn seaborn psutil
```

ساختار پوشه‌ها:

```
chest_xray/
├── train/
│   ├── NORMAL/
│   └── PNEUMONIA/
├── test/
│   ├── NORMAL/
│   └── PNEUMONIA/
└── val/
    ├── NORMAL/
    └── PNEUMONIA/
```

اجرای کد:

```python
# اجرای تمام سلول‌های نوت‌بوک به ترتیب
# یا اجرای مستقیم از ترمینال:
python x_ray.py
```

⚠️ محدودیت‌ها

1. اندازه داده‌ها: حجم داده‌های آموزشی محدود است
2. تنوع داده‌ها: داده‌ها از یک منبع جمع‌آوری شده‌اند
3. عمومیت‌پذیری: مدل نیاز به validation روی داده‌های مستقل دارد
4. منابع محاسباتی: مدل برای CPU بهینه شده اما آموزش زمان‌بر است

🔮 توسعه آینده

· استفاده از معماری‌های پیشرفته‌تر مانند ResNet یا EfficientNet
· افزایش حجم داده‌های آموزشی
· بهینه‌سازی بیشتر برای کاهش زمان آموزش
· توسعه رابط کاربری برای استفاده آسان

📚 مراجع

1. مجموعه داده Chest X-Ray Images (Pneumonia) از Kaggle
2. مستندات TensorFlow و Keras
3. مقالات مرتبط با تشخیص پنومونی از تصاویر اشعه ایکس

👥 مشارکت

پروژه متن‌باز است و مشارکت‌ها مورد استقبال قرار می‌گیرد. لطفاً ابتدا یک Issue ایجاد کنید یا Pull Request ارسال نمایید.

📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

---

توجه: این مدل برای اهداف تحقیقاتی و آموزشی توسعه یافته است و نباید به‌عنوان جایگزین تشخیص پزشکی استفاده شود. همیشه با پزشک متخصص مشورت کنید.
